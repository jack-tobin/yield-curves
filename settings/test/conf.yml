env: test

db:
  django_test:
    name: django_test
    user: test_username
    password: none_set
    host: test_host
    port: test_port
  yield_curves:
    name: yield_curves
    user: postgres
    password: postgres
    host: localhost
    port: 5432

debug: false
